import React from 'react';
import RecipeProgress from '../components/RecipeProgress';

const RecipeInProgress = () => (
  <section className="recepeInProgress">
    <RecipeProgress />
  </section>
);

export default RecipeInProgress;
