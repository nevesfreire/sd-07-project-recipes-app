import React from 'react';
import context from '../contextAPI/context';

function ListCard({ array }) {
  return (
    {
      array.map(cards, index) => {
        <div key={cards.id}>
      }
    }
  )
}