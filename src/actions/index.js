import {
  renderSearchIcon,
  changePageTitle,
  showBar,
  hideBar,
} from './header';

import {
  fetchCards,
} from './mainpage';

export default {
  renderSearchIcon,
  changePageTitle,
  showBar,
  hideBar,
  fetchCards,
};
