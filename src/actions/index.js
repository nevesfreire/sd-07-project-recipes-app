import {
  renderSearchIcon,
  changePageTitle,
  showBar,
  hideBar,
} from './header';

import {
  fetchMeals,
} from './mainpage';

export default {
  renderSearchIcon,
  changePageTitle,
  showBar,
  hideBar,
  fetchMeals,
};
